# Pinned so a figure re-captured next year runs the same code.
# bootstrap.sh installs these into tools/shots/.venv.
playwright==1.63.0
selenium==4.49.0     # only for Selenium Manager, which downloads Chrome for Testing
PyYAML==6.0.1
pillow==12.3.0
